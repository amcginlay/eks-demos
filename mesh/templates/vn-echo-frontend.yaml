apiVersion: appmesh.k8s.aws/v1beta2
kind: VirtualNode
metadata:
  name: vn-echo-frontend
spec:
  awsName: vn-echo-frontend
  podSelector:
    matchLabels:
      app: echo-frontend
  listeners:
    - portMapping:
        port: 80
        protocol: http
  serviceDiscovery:
    dns:
      hostname: echo-frontend
  backends:
    - virtualService:
        virtualServiceRef:
          name: vs-echo-backend

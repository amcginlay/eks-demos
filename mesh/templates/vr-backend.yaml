kind: VirtualRouter
metadata:
  name: vr-backend
spec:
  awsName: vr-backend
  listeners:
    - portMapping:
        port: 80
        protocol: http
  routes:
    - name: vrr-backend
      httpRoute:
        match:
          prefix: /
        action:
          weightedTargets:
          - virtualNodeRef:
              name: vn-backend-blue
            weight: 100
          - virtualNodeRef:
              name: vn-backend-green
            weight: 0
